<Window x:Class="WpfApp1.AccessWin"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d" WindowStartupLocation="CenterScreen"
        Title="AMONIC Airlines Automation System" Height="800" Width="1200" ResizeMode="NoResize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        <ToolBar Grid.Row="0">
            <Button x:Name="Add" Content="Add user" Click="AddUser_Click" FontSize="20"/>
            <Separator />
            <Button x:Name="ExitBtn" Content="Exit" Click="ExitBtn_Click" IsCancel="True" FontSize="20"/>
        </ToolBar>
        <Frame x:Name="Load" NavigationUIVisibility="Hidden" Grid.Row="1"/>
    </Grid>
</Window>
